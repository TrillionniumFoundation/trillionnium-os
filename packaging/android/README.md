# Android packaging boundary

Android product packaging is owned by the source-built product graph and the
measured Codex launcher under `../codex-android/`. This directory carries no
standalone bridge application or unsigned application scaffold.

Historical application-bridge material is recoverable only from the external
read-only 2026-08-07 retirement archive and is not a product input.
