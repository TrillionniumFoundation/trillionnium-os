self-hosted-runner:
  # The target-evidence workflow deliberately uses organization-managed
  # labels for L2-L6 lanes.  Keep them explicit so actionlint validates the
  # workflow expressions without treating the labels as typos.
  labels:
    - owner-open-r5-l2
    - owner-open-r5-l3
    - owner-open-r5-l4
    - owner-open-r5-l5
    - owner-open-r5-l6

config-variables: null
