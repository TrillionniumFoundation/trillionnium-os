self-hosted-runner:
  # Keep the desktop label explicit so the build/device lane cannot be routed
  # to the ROG runner in the same organization runner group.
  labels:
    - self-hosted
    - linux
    - x64
    - desktop

config-variables: null
