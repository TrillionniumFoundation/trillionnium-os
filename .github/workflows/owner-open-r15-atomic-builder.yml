name: Owner-Open R15 atomic source builder
on:
  push:
    branches: [ops/owner-open-r15-atomic-builder-20260831]
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          persist-credentials: true
          ref: ${{ github.sha }}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.13"
      - uses: dtolnay/rust-toolchain@d8dfd7155fea72af4ad34a8fe97364d210e2d020
        with:
          toolchain: "1.93.0"
          components: rustfmt, clippy
      - shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "ops/owner-open-r15-atomic-builder-20260831"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          cp .github/owner-open-r15-runtime-hardening.patch.gz /tmp/r15.patch.gz
          gzip -dc .github/owner-open-r15-builder.sh.gz > /tmp/r15-builder.sh
          chmod 700 /tmp/r15-builder.sh
          /tmp/r15-builder.sh /tmp/r15.patch.gz
