name: ChatGPT temporary control

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quiesced:
    runs-on: ubuntu-24.04
    steps:
      - run: printf '%s\n' 'temporary control branch is quiesced'
