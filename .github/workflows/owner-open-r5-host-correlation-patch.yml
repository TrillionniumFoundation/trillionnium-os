name: one-shot owner-open R5 exact Host correlation closure

on:
  push:
    branches:
      - codex/owner-open-r5-gap-closure-20260829
    paths:
      - ".github/workflows/owner-open-r5-host-correlation-patch.yml"

permissions:
  contents: write

jobs:
  patch-test-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: codex/owner-open-r5-gap-closure-20260829
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - uses: dtolnay/rust-toolchain@1.93.0
        with:
          components: rustfmt
      - name: Patch exact Host and broker response correlation
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path: Path, old: str, new: str, label: str) -> None:
              text = path.read_text(encoding="utf-8")
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected one match, observed {count}")
              path.write_text(text.replace(old, new), encoding="utf-8")

          wire = Path("apps/trillionnium-owner-open-host/src/bin/r5_control_host_v7/wire.rs")
          replace_once(
              wire,
              '''fn build_job_frame(
              context: &JobContext,
              kind: &str,
              seq: u64,
              discriminator: &str,
              payload: Value,
          ) -> RunTurnFrame {
              RunTurnFrame {
          '''.replace("          ", ""),
              '''fn build_job_frame(
              context: &JobContext,
              kind: &str,
              seq: u64,
              discriminator: &str,
              payload: Value,
          ) -> RunTurnFrame {
              let mut payload = payload;
              if let Some(request_sha256) = context.request_sha256.as_ref() {
                  if let Some(object) = payload.as_object_mut() {
                      object.insert(
                          "request_sha256".to_string(),
                          Value::String(request_sha256.clone()),
                      );
                  }
              }
              RunTurnFrame {
          '''.replace("          ", ""),
              "canonical job digest injection",
          )
          replace_once(
              wire,
              "        stream_id: Some(context.stream_id.clone()),\n        turn_stream_id: None,\n",
              "        stream_id: Some(context.stream_id.clone()),\n        turn_stream_id: Some(context.key.scope.turn_stream_id.clone()),\n",
              "job turn stream envelope",
          )

          process = Path("apps/trillionnium-owner-open-host/src/bin/r5_control_host_v7/process.rs")
          replace_once(
              process,
              '''                Ok(frame) if is_job_frame(&frame.kind) => {
                                  if let Err(error) = handle_job_frame(
              '''.replace("              ", ""),
              '''                Ok(frame) if is_job_frame(&frame.kind) => {
                                  let error_context =
                                      job_error_context(&frame, &manager, &shell_executable);
                                  let error_operation_id = frame_payload_string(&frame, "operation_id");
                                  let error_attachment_id = frame_payload_string(&frame, "attachment_id");
                                  if let Err(error) = handle_job_frame(
              '''.replace("              ", ""),
              "capture job error correlation",
          )
          replace_once(
              process,
              '''                        deliver_job_error(
                                          None,
                                          "job_request_failed",
              '''.replace("              ", ""),
              '''                        deliver_job_error(
                                          error_context.as_ref(),
                                          error_operation_id.as_deref(),
                                          error_attachment_id.as_deref(),
                                          "job_request_failed",
              '''.replace("              ", ""),
              "correlated request error delivery",
          )
          replace_once(
              process,
              '''                    deliver_job_error(
                                      None,
                                      "turn_core_failed",
              '''.replace("              ", ""),
              '''                    deliver_job_error(
                                      None,
                                      None,
                                      None,
                                      "turn_core_failed",
              '''.replace("              ", ""),
              "generic core error arguments",
          )
          marker = "#[allow(clippy::too_many_arguments)]\nfn handle_job_frame<W: IoWrite>(\n"
          helper = r'''fn frame_payload_string(frame: &RunTurnFrame, name: &str) -> Option<String> {
              frame
                  .payload
                  .get(name)
                  .and_then(Value::as_str)
                  .map(str::to_string)
          }

          #[allow(clippy::too_many_arguments)]
          fn job_error_context_from_parts(
              manager: &JobManager,
              session_id: String,
              profile_id: String,
              task_id: String,
              turn_id: String,
              turn_stream_id: String,
              job_id: String,
              request_sha256: Option<String>,
          ) -> Option<JobContext> {
              for (label, value) in [
                  ("session_id", session_id.as_str()),
                  ("profile_id", profile_id.as_str()),
                  ("task_id", task_id.as_str()),
                  ("turn_id", turn_id.as_str()),
                  ("turn_stream_id", turn_stream_id.as_str()),
                  ("job_id", job_id.as_str()),
              ] {
                  validate_id(value, label).ok()?;
              }
              let key = JobKey::new(
                  JobScope::new(
                      session_id,
                      profile_id,
                      task_id,
                      turn_id,
                      turn_stream_id,
                  ),
                  job_id,
              );
              let context = JobContext {
                  stream_id: job_stream_id(&key),
                  key,
                  request_sha256,
              };
              Some(resolve_job_context(manager, context.clone()).unwrap_or(context))
          }

          fn job_error_context(
              frame: &RunTurnFrame,
              manager: &JobManager,
              shell_executable: &Path,
          ) -> Option<JobContext> {
              if frame.kind == FRAME_JOB_START {
                  if let Ok(decoded) = decode_job_start(frame, shell_executable) {
                      return Some(decoded.context);
                  }
                  let wire: JobStartWire = serde_json::from_value(frame.payload.clone()).ok()?;
                  return job_error_context_from_parts(
                      manager,
                      wire.session_id,
                      wire.profile_id,
                      wire.task_id,
                      wire.turn_id,
                      wire.turn_stream_id,
                      wire.job_id,
                      wire.request_sha256,
                  );
              }
              let wire: JobControlWire = serde_json::from_value(frame.payload.clone()).ok()?;
              job_error_context_from_parts(
                  manager,
                  wire.session_id,
                  wire.profile_id,
                  wire.task_id,
                  wire.turn_id,
                  wire.turn_stream_id,
                  wire.job_id,
                  wire.request_sha256,
              )
          }

          '''.replace("          ", "")
          replace_once(process, marker, helper + marker, "job error context helpers")
          replace_once(
              process,
              '''            let decoded = decode_job_start(&frame, shell_executable)?;
                          let context = decoded.context.clone();
                          let result = manager
              '''.replace("              ", ""),
              '''            let decoded = decode_job_start(&frame, shell_executable)?;
                          let context = decoded.context.clone();
                          let operation_id = decoded.request.operation_id.clone();
                          let result = manager
              '''.replace("              ", ""),
              "job start operation capture",
          )
          replace_once(
              process,
              "                start_payload(&result),\n",
              "                start_payload(&result, &operation_id),\n",
              "job start response operation",
          )
          replace_once(
              process,
              '''            let decoded = decode_job_control(&frame)?;
                          let context = resolve_job_context(manager, decoded.context)?;
                          let inspection = if frame.kind == FRAME_JOB_ATTACH {
              '''.replace("              ", ""),
              '''            let decoded = decode_job_control(&frame)?;
                          let context = resolve_job_context(manager, decoded.context)?;
                          let response_operation_id = decoded.operation_id.clone();
                          let response_attachment_id = decoded.attachment_id.clone();
                          let inspection = if frame.kind == FRAME_JOB_ATTACH {
              '''.replace("              ", ""),
              "inspect and attach correlation capture",
          )
          replace_once(
              process,
              '''                let attachment_id = decoded
                                  .attachment_id
                                  .as_deref()
              '''.replace("              ", ""),
              '''                let attachment_id = response_attachment_id
                                  .as_deref()
              '''.replace("              ", ""),
              "attach identity source",
          )
          replace_once(
              process,
              '''                &inspection,
                              &durable_records,
                              max_frame_bytes,
              '''.replace("              ", ""),
              '''                &inspection,
                              &durable_records,
                              response_operation_id.as_deref(),
                              response_attachment_id.as_deref(),
                              max_frame_bytes,
              '''.replace("              ", ""),
              "inspection response correlation arguments",
          )
          replace_once(
              process,
              '''            let response = response_frame(
                              state,
                              FRAME_JOB_DETACH_RESULT,
                              attachment_id,
                              json!({
                                  "status": "detached",
                                  "attachment_id": attachment_id,
                                  "automatic_redispatch": false
                              }),
                          );
              '''.replace("              ", ""),
              '''            let discriminator = format!(
                              "detach-{attachment_id}-{}",
                              decoded.operation_id.as_deref().unwrap_or("no-operation")
                          );
                          let response = response_frame(
                              state,
                              FRAME_JOB_DETACH_RESULT,
                              &discriminator,
                              json!({
                                  "status": "detached",
                                  "operation_id": decoded.operation_id.as_deref(),
                                  "attachment_id": attachment_id,
                                  "automatic_redispatch": false
                              }),
                          );
              '''.replace("              ", ""),
              "detach response correlation",
          )
          replace_once(
              process,
              '''fn bounded_inspection_frame(
                  state: &mut JobDeliveryState,
                  kind: &str,
                  inspection: &JobInspection,
                  durable_records: &[Value],
                  max_frame_bytes: usize,
              ) -> Result<RunTurnFrame, String> {
              '''.replace("              ", ""),
              '''fn bounded_inspection_frame(
                  state: &mut JobDeliveryState,
                  kind: &str,
                  inspection: &JobInspection,
                  durable_records: &[Value],
                  operation_id: Option<&str>,
                  attachment_id: Option<&str>,
                  max_frame_bytes: usize,
              ) -> Result<RunTurnFrame, String> {
              '''.replace("              ", ""),
              "bounded inspection signature",
          )
          replace_once(
              process,
              '''            "durable_has_more": end < durable_records.len(),
                          "read_only": true,
              '''.replace("              ", ""),
              '''            "durable_has_more": end < durable_records.len(),
                          "operation_id": operation_id,
                          "attachment_id": attachment_id,
                          "read_only": true,
              '''.replace("              ", ""),
              "inspection payload correlation",
          )
          replace_once(
              process,
              '''        let frame = response_frame(
                          state,
                          kind,
                          &format!("{}-{start}-{end}", inspection.inclusive_cursor),
                          payload,
                      );
              '''.replace("              ", ""),
              '''        let discriminator = format!(
                          "{}-{start}-{end}-{}-{}",
                          inspection.inclusive_cursor,
                          operation_id.unwrap_or("no-operation"),
                          attachment_id.unwrap_or("no-attachment")
                      );
                      let frame = response_frame(state, kind, &discriminator, payload);
              '''.replace("              ", ""),
              "inspection event identity correlation",
          )
          replace_once(
              process,
              "fn start_payload(result: &JobStartResult) -> Value {\n",
              "fn start_payload(result: &JobStartResult, operation_id: &str) -> Value {\n",
              "start payload signature",
          )
          replace_once(
              process,
              '''        "snapshot": &result.snapshot,
                      "replay_status": &result.replay_status,
              '''.replace("              ", ""),
              '''        "snapshot": &result.snapshot,
                      "replay_status": &result.replay_status,
                      "operation_id": operation_id,
              '''.replace("              ", ""),
              "start payload operation binding",
          )
          replace_once(
              process,
              '''fn deliver_job_error<W: IoWrite>(
                  context: Option<&JobContext>,
                  code: &str,
              '''.replace("              ", ""),
              '''fn deliver_job_error<W: IoWrite>(
                  context: Option<&JobContext>,
                  operation_id: Option<&str>,
                  attachment_id: Option<&str>,
                  code: &str,
              '''.replace("              ", ""),
              "job error signature",
          )
          replace_once(
              process,
              '''    let frame = match context {
                      Some(context) => build_job_frame(
                          context,
                          "job.error",
                          0,
                          code,
                          json!({
                              "code": code,
                              "message": message,
                              "automatic_redispatch": false
                          }),
                      ),
              '''.replace("              ", ""),
              '''    let discriminator = format!(
                      "{code}-{}-{}",
                      operation_id.unwrap_or("no-operation"),
                      attachment_id.unwrap_or("no-attachment")
                  );
                  let frame = match context {
                      Some(context) => build_job_frame(
                          context,
                          "job.error",
                          0,
                          &discriminator,
                          json!({
                              "code": code,
                              "message": message,
                              "operation_id": operation_id,
                              "attachment_id": attachment_id,
                              "automatic_redispatch": false
                          }),
                      ),
              '''.replace("              ", ""),
              "correlated job error frame",
          )
          replace_once(
              process,
              '''            payload: json!({
                              "code": code,
                              "message": message,
                              "automatic_redispatch": false
                          }),
              '''.replace("              ", ""),
              '''            payload: json!({
                              "code": code,
                              "message": message,
                              "operation_id": operation_id,
                              "attachment_id": attachment_id,
                              "automatic_redispatch": false
                          }),
              '''.replace("              ", ""),
              "generic job error payload correlation",
          )

          broker_runtime = Path("tools/owner-open/owner_open_broker_runtime.py")
          replace_once(
              broker_runtime,
              '''def response_matches(request: "Request", frame: dict[str, Any]) -> bool:
                  kind = frame.get("kind")
                  if kind not in request.expected_kinds:
                      return False
                  actual = frame_correlation(frame)
                  if request.expected_job_id is not None and actual["job_id"] != request.expected_job_id:
                      return False
                  for name, expected in request.correlation.items():
                      if expected is not None and actual.get(name) != expected:
                          return False
                  return True
              '''.replace("              ", ""),
              '''def correlation_matches(request: "Request", frame: dict[str, Any]) -> bool:
                  actual = frame_correlation(frame)
                  if request.expected_job_id is not None and actual["job_id"] != request.expected_job_id:
                      return False
                  for name, expected in request.correlation.items():
                      if expected is not None and actual.get(name) != expected:
                          return False
                  return True


              def response_matches(request: "Request", frame: dict[str, Any]) -> bool:
                  kind = frame.get("kind")
                  return kind in request.expected_kinds and correlation_matches(request, frame)
              '''.replace("              ", ""),
              "shared strict correlation predicate",
          )

          broker = Path("tools/owner-open/owner_open_connection_broker.py")
          replace_once(
              broker,
              '''    Client,
                  Request,
                  frame_correlation,
              '''.replace("              ", ""),
              '''    Client,
                  Request,
                  correlation_matches,
                  frame_correlation,
              '''.replace("              ", ""),
              "broker strict correlation import",
          )
          replace_once(
              broker,
              '''                direct_error = frame["kind"] in {"host.error", "job.error"}
                              if direct_error or response_matches(active, frame):
              '''.replace("              ", ""),
              '''                direct_error = frame["kind"] in {"host.error", "job.error"}
                              if response_matches(active, frame) or (
                                  direct_error and correlation_matches(active, frame)
                              ):
              '''.replace("              ", ""),
              "direct error correlation gate",
          )

          strict_test = Path("tools/tests/test_owner_open_broker_correlation_strict.py")
          replace_once(
              strict_test,
              "from owner_open_broker_runtime import Request, frame_correlation, response_matches\n",
              "from owner_open_broker_runtime import (\n    Request,\n    correlation_matches,\n    frame_correlation,\n    response_matches,\n)\n",
              "strict test correlation import",
          )
          replace_once(
              strict_test,
              '''    def test_same_kind_and_job_but_wrong_turn_cannot_cross_deliver(self) -> None:
                      response = self.response("write-new")
                      response["turn_id"] = "old-turn"
                      response["payload"]["turn_id"] = "old-turn"
                      self.assertFalse(response_matches(self.request(), response))
              '''.replace("              ", ""),
              '''    def test_same_kind_and_job_but_wrong_turn_cannot_cross_deliver(self) -> None:
                      response = self.response("write-new")
                      response["turn_id"] = "old-turn"
                      response["payload"]["turn_id"] = "old-turn"
                      self.assertFalse(response_matches(self.request(), response))

                  def test_exact_correlated_direct_error_is_eligible_for_the_active_request(self) -> None:
                      response = self.response("write-new")
                      response["kind"] = "job.error"
                      self.assertTrue(correlation_matches(self.request(), response))
                      self.assertFalse(response_matches(self.request(), response))

                  def test_stale_direct_error_cannot_steal_active_request_ownership(self) -> None:
                      response = self.response("write-old")
                      response["kind"] = "job.error"
                      self.assertFalse(correlation_matches(self.request(), response))
              '''.replace("              ", ""),
              "strict direct error regressions",
          )

          jobs_test = Path("apps/trillionnium-owner-open-host/tests/r5_jobs.rs")
          replace_once(
              jobs_test,
              '''    assert!(
                      frames
                          .iter()
                          .any(|frame| frame["kind"] == "job.start.result")
                  );
                  assert!(frames.iter().any(|frame| frame["kind"] == "job.started"));
              '''.replace("              ", ""),
              '''    let start = frames
                      .iter()
                      .find(|frame| frame["kind"] == "job.start.result")
                      .expect("job.start.result");
                  assert_eq!(start["payload"]["operation_id"], "start-job-pipe");
                  let request_sha256 = start["payload"]["request_sha256"]
                      .as_str()
                      .expect("canonical job request digest");
                  assert_eq!(request_sha256.len(), 64);
                  assert!(request_sha256.bytes().all(|byte| byte.is_ascii_hexdigit()));
                  assert!(frames.iter().any(|frame| frame["kind"] == "job.started"));
              '''.replace("              ", ""),
              "job start exact correlation assertion",
          )
          replace_once(
              jobs_test,
              '''    assert!(frames.iter().any(|frame| {
                      frame["kind"] == "job.result" && frame["payload"]["terminal_kind"] == "exited"
                  }));
                  assert!(
              '''.replace("              ", ""),
              '''    assert!(frames.iter().any(|frame| {
                      frame["kind"] == "job.result" && frame["payload"]["terminal_kind"] == "exited"
                  }));
                  let control_operations = frames
                      .iter()
                      .filter(|frame| frame["kind"] == "job.control.result")
                      .filter_map(|frame| frame["payload"]["operation_id"].as_str())
                      .collect::<Vec<_>>();
                  assert!(control_operations.contains(&"write-job-pipe"));
                  assert!(control_operations.contains(&"close-job-pipe"));
                  for frame in frames.iter().filter(|frame| {
                      frame["kind"]
                          .as_str()
                          .is_some_and(|kind| kind.starts_with("job."))
                  }) {
                      assert_eq!(frame["turn_stream_id"], "turn-stream-jobs");
                      assert_eq!(frame["session_id"], "session-jobs");
                      assert_eq!(frame["profile_id"], "owner-open");
                      assert_eq!(frame["task_id"], "task-jobs");
                      assert_eq!(frame["turn_id"], "turn-jobs");
                      assert_eq!(frame["job_id"], "job-pipe");
                      assert_eq!(frame["payload"]["request_sha256"], request_sha256);
                  }
                  assert!(
              '''.replace("              ", ""),
              "all job frame lineage and digest assertions",
          )
          new_test = r'''
          #[test]
          fn attach_and_invalid_control_echo_exact_correlation() {
              let directory = tempfile::tempdir().unwrap();
              let provider = directory.path().join("provider.sh");
              let provider_marker = directory.path().join("provider-started");
              let job_store = directory.path().join("jobs.jsonl");
              provider_fixture(&provider, &provider_marker);

              let frames = vec![
                  job_start("sleep 0.2"),
                  serde_json::json!({
                      "kind": "job.attach",
                      "seq": 2,
                      "direction": "client_to_host",
                      "payload": {
                          "session_id": "session-jobs",
                          "profile_id": "owner-open",
                          "task_id": "task-jobs",
                          "turn_id": "turn-jobs",
                          "turn_stream_id": "turn-stream-jobs",
                          "job_id": "job-pipe",
                          "operation_id": "attach-job-pipe",
                          "attachment_id": "attachment-job-pipe",
                          "inclusive_cursor": 0,
                          "limit": 16
                      }
                  })
                  .to_string(),
                  serde_json::json!({
                      "kind": "job.resize",
                      "seq": 3,
                      "direction": "client_to_host",
                      "payload": {
                          "session_id": "session-jobs",
                          "profile_id": "owner-open",
                          "task_id": "task-jobs",
                          "turn_id": "turn-jobs",
                          "turn_stream_id": "turn-stream-jobs",
                          "job_id": "job-pipe",
                          "operation_id": "resize-pipe-job",
                          "rows": 40,
                          "cols": 120
                      }
                  })
                  .to_string(),
              ];
              let output = run_core(&provider, &job_store, &frames);
              let frames = decoded(&output);
              assert!(!provider_marker.exists());

              let start = frames
                  .iter()
                  .find(|frame| frame["kind"] == "job.start.result")
                  .expect("job.start.result");
              let request_sha256 = start["payload"]["request_sha256"]
                  .as_str()
                  .expect("canonical job request digest");
              let attach = frames
                  .iter()
                  .find(|frame| frame["kind"] == "job.attach.result")
                  .expect("job.attach.result");
              assert_eq!(attach["payload"]["operation_id"], "attach-job-pipe");
              assert_eq!(
                  attach["payload"]["attachment_id"],
                  "attachment-job-pipe"
              );
              assert_eq!(attach["payload"]["request_sha256"], request_sha256);
              assert_eq!(attach["turn_stream_id"], "turn-stream-jobs");

              let error = frames
                  .iter()
                  .find(|frame| {
                      frame["kind"] == "job.error"
                          && frame["payload"]["operation_id"] == "resize-pipe-job"
                  })
                  .expect("correlated job.error");
              assert_eq!(error["payload"]["request_sha256"], request_sha256);
              assert_eq!(error["turn_stream_id"], "turn-stream-jobs");
              assert_eq!(error["job_id"], "job-pipe");
          }

          '''.replace("          ", "")
          replace_once(
              jobs_test,
              "#[test]\nfn completed_job_is_not_redispatched_after_a_new_core_process() {\n",
              new_test + "#[test]\nfn completed_job_is_not_redispatched_after_a_new_core_process() {\n",
              "attach and error correlation integration test",
          )

          jobs_doc = Path("docs/protocols/owner-open-jobs-v1.md")
          replace_once(
              jobs_doc,
              "Every effectful response binds the exact `operation_id` and request digest.\nAll job frames carry the complete job scope and `job_id`.\n",
              """Every effectful response binds the exact `operation_id` and canonical job\n`request_sha256`. All job frames carry the complete job scope,\n`turn_stream_id`, `job_id` and canonical job digest once that digest exists.\n\nDirect-response correlation is closed as follows:\n\n| Request | Direct result | Required echoed identity |\n| --- | --- | --- |\n| `job.start` | `job.start.result` | full scope, `job_id`, start `operation_id`, canonical request digest |\n| `job.inspect` | `job.inspect.result` | full scope, `job_id`, optional supplied `operation_id`, canonical request digest |\n| `job.attach` | `job.attach.result` | full scope, `job_id`, optional supplied `operation_id`, `attachment_id`, canonical request digest |\n| `job.detach` | `job.detach.result` | full scope, `job_id`, optional supplied `operation_id`, `attachment_id`, canonical request digest |\n| write/resize/close/kill | `job.control.result` | full scope, `job_id`, exact `operation_id`, canonical request digest |\n| rejected job request | `job.error` | every mechanically recoverable field from the rejected request; no field is treated as a wildcard |\n\nAn error that cannot recover enough exact request correlation remains an\nobservation and cannot resolve a different active Broker request. Missing\ncorrelation therefore fails closed into timeout/uncertainty rather than\ncross-delivering an error to the wrong operation.\n""",
              "job response correlation matrix",
          )

          broker_doc = Path("docs/protocols/owner-open-multi-connection-broker-v1.md")
          replace_once(
              broker_doc,
              """`host.error` and `job.error` remain direct failure frames only while one request\nis active. They are not proof that an external effect did or did not start; the\nbroker records the observed error without retry.\n""",
              """`host.error` and `job.error` may resolve the active request only when every\nnon-null request correlation field is present and exact. Merely having one\nactive request is insufficient. An uncorrelated or stale error is broadcast as\nan observation, cannot steal owner-result delivery, and leaves the request\nunresolved until an exact result or the finite uncertainty deadline. Even an\nexactly correlated error is not proof that an external effect did or did not\nstart; the broker records the observed error without retry.\n""",
              "broker direct error rule",
          )

          Path(".github/workflows/owner-open-r5-host-correlation-patch.yml").unlink()
          PY
      - name: Format and test exact Host job path
        run: |
          set -euo pipefail
          cargo fmt --all
          cargo test --locked --package trillionnium-owner-open-host --test r5_jobs
          cargo test --locked --package trillionnium-owner-open-host --test r5_stream_flow_control
      - name: Test strict broker ownership and durability paths
        run: |
          set -euo pipefail
          python3 -m unittest discover \
            -s tools/tests \
            -p 'test_owner_open_broker_*.py' \
            -v
          python3 -m unittest discover \
            -s tools/tests \
            -p 'test_owner_open_connection_broker.py' \
            -v
      - name: Verify active R5 machine truth
        run: |
          set -euo pipefail
          python3 tools/verify-owner-open-r5.py --json >/tmp/owner-open-r5.json
          python3 tools/verify-owner-open-r5-gap-evidence.py --json >/tmp/owner-open-r5-gap-evidence.json
          git diff --check
      - name: Commit exact Host and broker correlation closure
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            apps/trillionnium-owner-open-host/src/bin/r5_control_host_v7/wire.rs \
            apps/trillionnium-owner-open-host/src/bin/r5_control_host_v7/process.rs \
            apps/trillionnium-owner-open-host/tests/r5_jobs.rs \
            tools/owner-open/owner_open_broker_runtime.py \
            tools/owner-open/owner_open_connection_broker.py \
            tools/tests/test_owner_open_broker_correlation_strict.py \
            docs/protocols/owner-open-jobs-v1.md \
            docs/protocols/owner-open-multi-connection-broker-v1.md \
            .github/workflows/owner-open-r5-host-correlation-patch.yml
          git diff --cached --check
          git commit -m "fix(r5): bind exact Host and broker response correlation"
          git push origin HEAD:codex/owner-open-r5-gap-closure-20260829
